# 13 — E2E Recertification

**Status:** pending evidence from harness

Abbreviated REM dossier path: product + dossier with tag `REM-YYYYMMDD-xxxx` (`REM-PRODUCT-CREATE`, `REM-DOSSIER-CREATE`). Observation shape: `OBS-OPEN-SHAPE`.
