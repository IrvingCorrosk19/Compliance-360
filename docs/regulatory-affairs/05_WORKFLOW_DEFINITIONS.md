# 05 — Workflow Definitions

State machine en `RegistrationDossier` (`AllowedTransitions`).  
Workflow Engine puede adjuntarse vía `AttachWorkflow` (campo `WorkflowInstanceId`) — definición BPM adicional opcional en fases siguientes.
